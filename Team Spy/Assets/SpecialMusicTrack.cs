using UnityEngine;
using System.Collections;

public class SpecialMusicTrack : MonoBehaviour {
	//Unique music for a given level.
	//Attach this script anywhere (although the static control object is probably best).
	public AudioClip clip;
}
